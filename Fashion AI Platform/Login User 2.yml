info:
  name: Login User 2
  type: http
  seq: 5

http:
  method: POST
  url: http://127.0.0.1:4000/auth/login
  body:
    type: json
    data: |-
      {
        "email": "hrithik@example.com",
        "password": "Password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
